# Kconfig settings for PRU

if MACH_PRU

config PRU_SELECT
    bool
    default y
    select HAVE_GPIO

config BOARD_DIRECTORY
    string
    default "pru"

config CLOCK_FREQ
    int
    default 200000000

endif
